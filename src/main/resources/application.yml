spring:
  application:
    name: api_stock
  datasource:
    url: jdbc:mysql://localhost:3306/stock_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 9090


appDescription: "Stock API"
appVersion: "1.0.0"